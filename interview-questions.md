# Harness 面试题沉淀

## Agent 循环

### 为什么用原生 tool_calls 通道而不是提示词字符串约定？

参考回答要点：

- 提示词约定是"劝"，协议字段是"管"：模型可以不守暗号，但结构化字段
  由 API 保证形状，解析永远不会猜错。
- 字符串参数要靠启发式猜类型（`add 2 3` 里的 2 是数字还是文本？）；
  JSON 参数天生带类型，类型校验有 JSON Schema。
- 一轮多个工具调用、每次调用的唯一 ID（tool_call_id）与结果的对应关系，
  文本格式表达不了，结构化通道原生支持。
- 真实系统佐证：OpenAI / Anthropic / DeepSeek 全部提供原生工具通道，
  没有一家靠"让模型念暗号"。

面试官追问方向：

- wire 格式里 arguments 为什么是 JSON 字符串而不是 JSON 对象？
- tool_call_id 在回传结果时起什么作用？丢了会怎样？
- 模型一轮发起多个工具调用，应该并行执行还是串行？出错几个算几个还是全部重试？

### 步数用尽应该抛异常还是返回状态字段？

参考回答要点：

- 先区分"可预期的结局"和"真正的意外"：批处理 1000 个任务时，到步数上限是常态，
  用返回值（status: completed / max_steps）表达；bug、断网才配得上异常。
- 异常没有结构：调用方想区分失败原因、拿到最后一轮发言，就只能解析错误文案，脆弱。
- 状态字段可以带类型标注（Literal / Enum）配合类型检查器防拼错；
  批量调度时 `if result.status == ...` 也比层层 try/except 干净。
- 真实系统佐证：LLM API 返回 finish_reason: stop / length，而不是对"截断"抛错。

面试官追问方向：

- 什么情况下必须抛异常而不是返回状态？（调用链深处无法继续、需要中断多层调用时）
- status 用自由字符串、Literal 还是 Enum？运行时和静态检查的差异各是什么？

## 工具注册表

### 为什么需要 ToolRegistry？

参考回答要点：

- 将工具名称和具体函数实现解耦。
- Harness 只需要通过名称查找和调用，不需要知道每个工具的内部逻辑。
- 新增工具只需要注册，降低对核心循环的修改风险。
- 集中管理后，未来可以增加参数校验、权限控制、超时和日志。

面试官追问方向：

- 如果两个工具名称相同，应该覆盖还是报错？为什么？
- 如何限制某个任务只能调用部分工具？
- 工具函数抛出异常时，应该让整个 Agent 退出吗？

### `*args` 和 `**kwargs` 有什么作用？

参考回答要点：

- `*args` 接收任意数量的位置参数。
- `**kwargs` 接收任意数量的关键字参数。
- 在本项目中，它们让注册表可以统一调度参数形式不同的工具。
- 生产环境还需要额外做参数 Schema 校验，不能只依赖 Python 函数调用报错。

面试官追问方向：

- `*args` 和 `**kwargs` 在调用时如何展开？
- 为什么工具系统不能只接受固定的 `handler(a, b)`？
